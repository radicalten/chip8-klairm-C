CC := clang
SOURCES:= $(wildcard src/*.c)
OBJECTS:= $(notdir $(SOURCES:.c=.o))


all: $(BINARY)

$(BINARY): $(OBJECTS)
	$(CC) -o $@ $^

%.o:%.c
	$(CC) $(CFLAGS) -c -o $@ $^

clean:
	rm -rf $(BINARY) $(OBJECTS)
