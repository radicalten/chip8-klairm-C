name: OSX2
on:
  push:
    branches:
    - main
jobs:
  build:
    runs-on: macos-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Install SDL2
      run: |
        mkdir SDL
        cd SDL
        git clone --branch SDL2 --depth 1 https://github.com/libsdl-org/SDL.git
        CC=SDL/build-scripts/clang-fat.sh
    - name: Compile
      run: |
        make -f Makefile.osx
    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: Artifact
        path: |
          ./
